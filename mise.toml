[tools]
stylua = "latest"
"cargo:selene" = "latest"
just = "latest"
lazygit = "latest"
tree-sitter = "latest"

# once per clone: point git at committed hooks
[tasks.prepare]
run = "git config core.hooksPath .githooks"
