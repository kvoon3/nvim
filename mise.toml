[tools]
neovim = "0.12"
stylua = "2"
"cargo:selene" = "0"
just = "1"
lazygit = "0"
tree-sitter = "0"
"npm:@ls-lint/ls-lint" = "2"

# once per clone: install tracked hook into .git/hooks
[tasks.prepare]
run = "cp .githooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit"
